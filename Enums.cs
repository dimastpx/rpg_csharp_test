namespace Test;
public enum Effect
{
    Poison, Regeneration, Boost, Resistance, Weakness
}

public enum ItemType
{
    Damage, EffectToSelf, EffectToEnemy, Heal, EscapeHelper
}
public enum Property
{
    Venomous, Spikes
}